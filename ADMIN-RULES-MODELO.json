{
  "rules": {
    "administradores": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": false
      }
    },
    "barbeiros": {
      "$barbeiroId": {
        ".read": "auth != null && root.child('administradores').child(auth.uid).val() === true || auth != null && auth.uid === data.child('ownerUid').val()",
        ".write": "auth != null && root.child('administradores').child(auth.uid).val() === true || auth != null && auth.uid === data.child('ownerUid').val()",
        "ownerUid": {
          ".read": "auth != null && (root.child('administradores').child(auth.uid).val() === true || auth.uid === data.val())",
          ".write": "auth != null && root.child('administradores').child(auth.uid).val() === true"
        }
      }
    }
  }
}
